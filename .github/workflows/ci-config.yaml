name: Renovate config CI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'renovate.json'
      - 'renovate.json5'
      - '.github/renovate.json'
      - '.github/renovate.json5'
      - '.gitlab/renovate.json'
      - '.gitlab/renovate.json5'
      - '.renovaterc'
      - '.renovaterc.json'
      - '.renovaterc.json5'
      - 'package.json'

jobs:
  build:
    uses: ./renovate-ci.yaml
